<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="175dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_category_layout"
    android:layout_height="wrap_content"
    android:layout_marginEnd="12dp"
    android:orientation="vertical">

    <com.jcminarro.roundkornerlayout.RoundKornerRelativeLayout
        android:id="@+id/roundKornerRelativeLayout"
        android:layout_width="match_parent"
        android:layout_height="75dp"
        app:corner_radius="10dp">

    <ImageView
        android:id="@+id/travel_band_image_view"
        android:layout_width="match_parent"
        android:layout_height="75dp"
        android:src="@drawable/rounded_no_border_shape"
        tools:srcCompat="@tools:sample/backgrounds/scenic"
        android:scaleType="centerCrop"/>

    </com.jcminarro.roundkornerlayout.RoundKornerRelativeLayout>

    <TextView
        android:id="@+id/travel_band_title"
        android:layout_width="match_parent"
        android:layout_marginTop="6dp"
        android:layout_height="wrap_content"
        android:fontFamily="@font/nunito_regular"
        android:textColor="@color/colorMineShaft"
        android:textSize="16dp"
        tools:text="@string/item_category_title" />

</LinearLayout>